# Core package dependencies (mirrors pyproject.toml [dependencies])
torch>=2.0.0
transformer-lens>=1.14.0
einops>=0.6.0
numpy>=1.24.0

# Development / testing extras (mirrors pyproject.toml [dev])
pytest>=7.0.0

# Optional: SAE feature attribution
# sae-lens>=3.0

# Optional: interactive dashboard
# streamlit>=1.32.0
# plotly>=5.18.0
